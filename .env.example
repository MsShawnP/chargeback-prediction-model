# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# Cinderhaven Postgres (via flyctl proxy)
# Start proxy first: flyctl proxy 5432 -a cinderhaven-data-platform
DATABASE_URL=postgresql://user:pass@localhost:5432/cinderhaven
